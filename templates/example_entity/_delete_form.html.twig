<form method="post" action="{{ path('app_example_entity_delete', {'id': example_entity.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ example_entity.id) }}">
    <button class="btn">Delete</button>
</form>
